# Specform ReadTheDocs skeleton

This folder contains a minimal Sphinx + MyST docs site suitable for ReadTheDocs.

## Local build

```bash
pip install -r docs/requirements.txt
sphinx-build -b html docs docs/_build/html
```

Open:

- `docs/_build/html/index.html`
